PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/approvaliq
JWT_SECRET=replace-with-a-strong-secret
# Comma-separated list. Leave blank to allow all origins.
CORS_ORIGIN=http://localhost:5173
# Absolute or relative path for persistent uploads in deployment.
UPLOADS_DIR=uploads
